***** Comandos SQL ********


CREATE TABLE FUNCIONARIOS (
	ID INT 				NOT NULL,
    NOME VARCHAR(20)	NOT NULL,
    IDADE INT 			NOT NULL,
    CIDADE CHAR (25),
    SALARIO DECIMAL (18, 2),
    PRIMARY KEY (ID)
);

DESC FUNCIONARIOS;

INSERT INTO FUNCIONARIOS (ID,NOME,IDADE,CIDADE,SALARIO)
VALUES (1,'PELE',32,'ROMA',2000.00);

INSERT INTO FUNCIONARIOS (ID,NOME,IDADE,CIDADE,SALARIO)
VALUES (2,'ZICO',25,'PARIS',1000.00);

INSERT INTO FUNCIONARIOS (ID,NOME,IDADE,CIDADE,SALARIO)
VALUES (3,'RIVELINO',23,'SANTIAGO',4000.00);

INSERT INTO FUNCIONARIOS (ID,NOME,IDADE,CIDADE,SALARIO)
VALUES (4,'GARRINCHA',29,'VIENNA',5500.00);

INSERT INTO FUNCIONARIOS (ID,NOME,IDADE,CIDADE,SALARIO)
VALUES (5,'JAIR',64,'AMSTERDÃ',8000.00);

INSERT INTO FUNCIONARIOS (ID,NOME,IDADE,CIDADE,SALARIO)
VALUES (6,'ANTONIO',64,'ROMA',8000.00);

select * from funcionarios;

SELECT ID, NOME, SALARIO FROM FUNCIONARIOS WHERE SALARIO > 2000 AND IDADE < 55; 

UPDATE FUNCIONARIOS SET CIDADE = 'BOSTON' WHERE ID = 4;

DELETE FROM FUNCIONARIOS WHERE ID = 5;

SELECT DISTINCT CIDADE FROM FUNCIONARIOS;   #(DISTINCT= TIRA DUPLICIDADE)

SELECT COUNT(*) FROM FUNCIONARIOS;

SELECT CIDADE, SUM(SALARIO) FROM FUNCIONARIOS GROUP BY CIDADE;

SELECT CIDADE, SUM(SALARIO) FROM FUNCIONARIOS GROUP BY CIDADE HAVING SUM(SALARIO) > 2000; #FILTRAR PELO AGRUPAMENTO

***************************************************************************************************
# FULL TABLE SCAN: 
# QUANDO A TABELA É MUITO GRANDE USAR INDICES, POIS IRÁ DEMORAR PARA FAZER UMA VARREDURA COMPLETA

create index cidade_idx on FUNCIONARIOS(cidade);

# ou seja, ELE FAZ UM FULL INDEX SCAN, NÃO MAIS UM FULL TABLE SCAN
select cidade from funcionarios;


# Criar cópia de uma tabela
create table funcionarios_bkp as select * from funcionarios;

# remover a tabela de bkp
drop table funcionarios_bkp;

# apagar banco de dados
drop database titanicDB;
drop database cadastroDB;
